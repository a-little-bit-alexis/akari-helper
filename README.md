# Akari Helper

A React and TypeScript workspace for building an interactive helper and solver for Akari puzzles.

## Development

```sh
npm install
npm run dev
```

## Checks

```sh
./scripts/check.sh
```

The check script builds the app, runs ESLint with zero warnings, checks Prettier formatting, and runs Jest.
